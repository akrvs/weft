# M24

Status: complete, petnames in the address bar, label lists that hide, blur, warn, highlight
Started: 2026-09-22

## Goal

Fill the naming layer and give the reader a say in what they see. A
petname is the reader's own word for a key, typed in the address bar
before any domain. A label list is a labeler's signed statement about
records and keys; the browser subscribes to labelers and hides, blurs,
warns, or highlights what they label. Both are signed list records whose
head is a pointer, so they reuse the store, the relay, and the pull path
unchanged.

## Decisions

| Topic | Decision |
|---|---|
| Scope | Petnames and label lists together, CLI and browser. Gateway and relays ignore labels |
| Petname record | `kind = "petname"`, body `{ names: [ { key: bytes(32), name: text } ] }`, 0 to 512 entries sorted by `name`, names unique. A petname is 1 to 32 bytes, first `a-z`, then `a-z`, `0-9`, `-` |
| Label record | `kind = "label"`, body `{ labels: [ { key or record: bytes(32), value: text } ] }`, exactly one of `key` and `record`, `value` in the kind grammar, 0 to 512 entries sorted `key` before `record`, then subject bytes, then `value`, unique. A key label covers every record by that author. Values are open |
| Head | A list record is a whole snapshot. Its head is the author's pointer `petnames` or `labels`, the pointer rule. An empty list is how the last entry goes |
| Verify | Both kinds decode and check in `verify`; no authorization beyond section 7. Neither is reserved, so both are grantable |
| Privacy | Own petnames live in the home store and leave it only through `weft push`. Nothing publishes on change |
| Address bar | `Target::Petname { petname, name }`: a head with no dot that is not an address and fits the petname grammar, `name` defaulting to `home`. Resolves through the reader's own list head, local only, never pulled. An unknown petname is `Error::Petname`. Addresses are 60 characters, so a petname never parses as one, and a dot means a domain, so a petname never shadows DNS |
| Subscribed petnames | Import only. `weft petname import <key> [name]...` and a browser button copy entries from another list into the reader's own; existing names are kept and reported. No live fallback |
| Gateway | A petname target is 404; the operator's names are never served |
| Labels in the browser | `<home>/browser/labelers` (`<key address>` per line, at most 64) and `<home>/browser/actions` (`<value>\t<hide\|blur\|warn\|highlight>`), 0700 and 0600, rewritten whole. A page gathers hits on its record address and its author from each labeler's list head in the local store only; subscribing and a refresh button pull the heads. Strongest action wins: hide, blur, warn, highlight, then a plain badge for unmapped values |
| Petnames in the browser | The provenance panel shows the author's petname; a name field adds one. A Names view lists, adds, removes, and imports. Writes go over the existing socket: `put` of the list record, then `point` |
| Store socket | No new request. `put` already signs any non reserved kind for the browser key and granted apps; `point` stays browser only |
| CLI | `weft petname add <name> <key>`, `remove <name>`, `list [key]`, `import <key> [name]...`; `weft label add <address> <value>`, `remove <address> <value>`, `list [key]`. Each change writes a list record and its pointer, `--as` picks the signer, and prints both addresses for `weft push`. `list <key>` pulls |
| Dependencies | None new |

## Layout

```
docs/protocol.md                    section 8 limits, 9 kinds, 14 petnames, 15 labels
docs/design.md                      status rows for petnames and label lists
crates/core/src/petname.rs          Petnames, valid_petname, KIND, POINTER
crates/core/src/label.rs            Labels, Subject, KIND, POINTER
crates/core/src/verify.rs           two arms
crates/core/examples/vectors.rs     list vectors
vectors/lists.json                  new
crates/core/tests/vectors.rs        lists
crates/resolve/src/target.rs        Target::Petname
crates/resolve/src/resolver.rs      petnames, labels, petname resolution
crates/cli/src/main.rs              petname and label commands
crates/cli/tests/e2e.rs             petname and label round trips
crates/gateway/src                  404 on a petname
crates/browser/src/marks.rs         labelers and actions files beside history and bookmarks
crates/browser/src/lists.rs         page annotation, names and labels commands
crates/browser/src/main.rs          resolve returns the annotated view
crates/browser/ui                   provenance petname, label treatment, Names and Labels views
crates/browser/smoke.sh             petname in the bar, a labeled page
```

## Plan

1. Core: `petname.rs` and `label.rs` with check, encode, decode, draft,
   `from_record`; `verify` arms; vectors with every rejection; protocol
   sections.
2. Resolve: `Target::Petname` parse and display with negatives;
   `Resolver::petnames` over the own head offline, `labels(key)` over a
   labeler's head, `resolve` mapping a petname to `Named`.
3. CLI: petname and label commands through one list writer; e2e.
4. Gateway: petname target 404, test.
5. Browser: state files, page annotation, views, drive smoke with a
   petname opened from the bar and a labeled page warned and hidden;
   screenshots.
6. Close.

## Delivered

- `weft-core`: `petname.rs` and `label.rs`, each a whole snapshot list
  with check, encode, decode, draft, `from_record`, sorted insert and
  remove, and lookups (`key`, `name`, `on`); two arms in `verify`.
  `vectors/lists.json` holds 4 valid records and 22 rejections, one per
  rule; the earlier vectors did not move.
- `protocol.md` sections 14 and 15, limits for petnames and list sizes,
  section 9 naming both kinds as defined and grantable.
- `weft-resolve`: `Target::Petname`, `Resolver::petnames` over the
  reader's own head offline, `petnames_of` and `labels` for anyone with a
  pull, one `list` helper that rejects a list whose author is not the
  pointer's, `Error::Petname`; pages drop `weft:` links to a petname.
- `weft-gateway`: a petname is 404 and never resolved against the
  operator's list; a dotless path on a publisher host still means
  `host/path`.
- `weft`: `petname add|remove|list|import` and `label add|remove|list`
  through one list writer that signs the list and its pointer with one
  passphrase prompt; `next_pointer` shared with `point`.
- `weft-browser`: the provenance line and panel show the author's
  petname with a name field when there is none; names dialog (list, add,
  remove, import); labels dialog (follow by key or petname, refresh,
  unfollow, value to action); a banner per treatment, hide behind a
  "show anyway" button, blur lifted by a click; labelers and actions
  files in `marks.rs`.
- `smoke.sh` gains a labeler identity: the CLI imports and lists over a
  real relay, the browser imports `alice`, opens `alice` from the bar,
  hides then blurs `alice/blog`, and the gateway answers 404 for
  `/alice`. Screenshots refreshed; hidden and blurred states checked by
  eye.
- 164 tests, up from 156.

## Deviations from the plan

- The labelers and actions files live in `marks.rs` with history and
  bookmarks, one reader and writer for browser state, instead of a new
  `labels.rs`; the commands went to `lists.rs`.
- Pages may not link a petname: a page author cannot know the reader's
  list. Not in the plan; it fell out of the render test.
- The gateway's publisher fallback had to run before petname parsing,
  or `/posts` on a publisher host would have become a petname.
- Label entries sort by subject kind, bytes, then value rather than by
  canonical encoding: one order a reader can state, and `Address` order.
- `design.md` "Status at 0.1" became "Status", measured against `main`.
- The handoff shed detail that lives in code to stay under 120 lines.

Unverified: the warn and highlight treatments are covered by the action
order test and the shared banner code, not driven in the smoke.

## Open for M25

- Web of trust: follows and endorsements as signed lists behind
  pointers, the same shape as petnames and labels, with a trust distance
  the browser could filter labelers by.

## Out of scope

- Label authoring in the browser; labelers publish from the CLI.
- Live fallback through other people's petname lists.
- Labels in the gateway or relays.
- Pointing by granted apps.

## Log

- 2026-09-22 Two rounds of questions: both kinds, signed own list,
  dotless petnames, browser only labels, import only, open values on
  records and keys, both grantable. Plan written.
- 2026-09-22 Core, vectors, protocol, resolver, gateway, CLI, browser,
  smoke. The smoke caught a `jq -e` false exit and a bookmark check that
  assumed the old navigation order; the marks test caught a record
  address accepted as a labeler. Verified, closed.
