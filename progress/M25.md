# M25

Status: complete, follows, trust distance, labelers gated by reach
Started: 2026-09-22

## Goal

Add the web of trust. A follow list is a reader's signed list of the
keys they vouch for, the same shape as petnames and labels. Walking
follows from the reader's own root gives each key a trust distance. The
browser shows the author's distance, lets the reader follow and unfollow,
and counts a labeler's hits only when the labeler is close enough. The
CLI edits follows and prints a distance with one shortest path.

## Decisions

| Topic | Decision |
|---|---|
| Record | `kind = "follow"`, body `{ follows: [ bytes(32) ] }`, 0 to 512 public keys sorted by bytes and unique. Head is the author's pointer `follows`. A whole snapshot, like `petname` and `label`. Grantable, not reserved |
| Endorsements | No second kind. An endorsement is a label on a key |
| Distance | 0 is the reader's root, 1 a key in the reader's list, then one per hop, at most 3. A key further out, or never reached, has no distance |
| Walk | Breadth first from the reader's root, lists read through `Resolver::list`, so each list is verified, authored by its key, and follows recovery redirects; a redirected key takes the distance of the key that named it. Lists are read in hop order, then key bytes order, at most `MAX_LISTS` 1024 of them. A list that fails to read or verify is skipped; only the reader's own list fails the walk |
| Pulls | An offline resolver walks the local store only; that is every page render. An online resolver pulls every list it reads; that is `weft trust --refresh`, the browser refresh button, and a follow |
| Labeler filter | `<home>/browser/reach`: `1`, `2`, `3`, or `any`, one line, default `2` when missing. A labeler counts only within reach. The Labels view shows each labeler's distance and whether it is in reach |
| Browser | The provenance panel shows `you`, `followed`, `2 hops`, `3 hops`, or nothing, and a follow or unfollow button. A Follows view lists, adds by key or petname, removes, imports, and refreshes. Writes are `put` then `point`, like petnames. The walk result is cached in the app for 60 s and dropped on every follow write and refresh |
| CLI | `weft follow add <key>`, `remove <key>`, `list [key]`, `import <key>`, through the M24 list writer; `add` pulls the new key's list. `weft trust [key] [--refresh]`: with a key, its distance and one shortest path; without, the count at each distance |
| Gateway, relays | Ignore follows. Relays store and serve them like any record |
| Dependencies | None new |

## Layout

```
docs/protocol.md                    section 8 limits, 9 kinds, 16 follows
docs/design.md                      status row, roadmap row
crates/core/src/follow.rs           Follows, KIND, POINTER, MAX_ENTRIES
crates/core/src/verify.rs           one arm
crates/core/examples/vectors.rs     follows vectors
vectors/follows.json                new
crates/core/tests/vectors.rs        follows
crates/resolve/src/trust.rs         Trust, MAX_DISTANCE, MAX_LISTS
crates/resolve/src/resolver.rs      follows_of, trust
crates/resolve/tests/pull.rs        walk, redirect, budget, bad list, pulls
crates/cli/src/lists.rs             follow and trust commands
crates/cli/tests/e2e.rs             follow round trip, trust distances
crates/browser/src/marks.rs         reach file
crates/browser/src/lists.rs         distance in the view, reach filter, follow commands
crates/browser/ui                   provenance distance and button, Follows view, reach select
crates/browser/smoke.sh             follow a labeler, reach filter, distance in the panel
```

## Plan

1. Core: `follow.rs` with check, encode, decode, draft, `from_record`,
   sorted insert and remove, `contains`; `verify` arm; `follows.json`
   with valid records and one rejection per rule; protocol section 16.
2. Resolve: `Resolver::follows_of`, `Trust` with `distance` and `path`,
   `Resolver::trust(root)`; tests over a local store for the chain, the
   hop cap, the budget, a bad list skipped, a redirect, and a pull.
3. CLI: `follow` and `trust` commands; e2e over three identities.
4. Browser: reach file, view distance and follow state, labeler filter,
   Follows view, cache; drive smoke with a followed labeler in reach
   and an unfollowed one out of reach; screenshots.
5. Close.

## Delivered

- `weft-core`: `follow.rs`, a whole snapshot list of keys with check,
  encode, decode, draft, `from_record`, sorted insert and remove,
  `contains`; one arm in `verify`. `vectors/follows.json` holds 3 valid
  records and 9 rejections; no earlier vector moved.
- `protocol.md` section 16, the kinds paragraph, and the limits table;
  `design.md` status row shipped, changed, and roadmap row M25.
- `weft-resolve`: `follows_of`, `trust.rs` with `Trust` (`distance`,
  `path`, `counts`, `lists`), `Resolver::trust` walking breadth first
  under `MAX_DISTANCE` 3 and `MAX_LISTS` 1024. Five tests: shortest
  distance and path, a bad list skipped and the own list failing, the
  list budget, a recovery redirect, an online pull kept for offline.
- `weft`: `follow add`, `remove`, `list [key]`, `import <key>` through
  the M24 list writer; `trust [key] [--refresh]`. The list commands
  moved into one flattened `lists::Command`. One e2e over three homes.
- `weft-browser`: `<home>/browser/reach` with `Reach`, default 2, junk
  refused; `View` carries `distance` and `followed`; labelers out of
  reach contribute no hits; `Cache` holds the walk 60 s and drops it on
  every follow write and refresh. Commands `follows`, `follow`,
  `unfollow`, `import_follows`, `refresh_trust`, `reach`, `set_reach`.
  UI: distance beside the petname, a trust row with a follow button, a
  Follows dialog, a reach select, and the labels dialog now says
  subscribe.
- `smoke.sh`: CLI trust before and after `--refresh`; in the browser a
  subscribed labeler out of reach until the author is followed, a hide
  applied once it is in reach, reach 1 dropping it, the Follows view,
  and refresh. Screenshots refreshed.
- Tests 164 to 172.

## Deviations from the plan

- `weft follow add` does not pull the new key's list; `trust --refresh`
  does. The browser follow button pulls that one list.
- Redirects apply to keys whose lists are read, distance 0 to 2; a key
  first seen at distance 3 is not redirected. Protocol 16 says so.

## Open for M26

- Relays rate limiting by distance from the operator, the other half of
  design 4.7.
- Recovery in the browser, the next dialog on the backlog.

## Out of scope

- Relays rate limiting by distance.
- Weighted or negative trust, distrust lists.
- Pulling lists during a page render.
- Follows in the gateway.

## Log

- 2026-09-22 One round of questions: follow list only, local walk
  capped at 3 hops, distance in the panel, labeler filter, CLI trust,
  browser editing. Plan written.
- 2026-09-22 Core, vectors, protocol, resolver, CLI, browser, smoke. The
  smoke caught a render race after the reach change and clippy a test
  with single letter names. Verified, closed.
